import React, { Component } from 'react';
class Category extends Component {

	// 

  render() {
    return (
    <div>
    	
        <h2>Category</h2>
      	
      	<div>identical to the default view, but filtered to only include posts with the selected category</div>

      </div>
    );
  }
}

export default Category;

